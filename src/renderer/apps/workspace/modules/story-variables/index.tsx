/**
 * Story Variables panel. Registered dynamically by the story scene editor (not a static module),
 * mirroring the Actions panel, so it carries the active story/scene as its payload.
 */
export { StoryVariablesPanel } from "./StoryVariablesPanel";
export { STORY_VARIABLES_PANEL_ID, type StoryVariablesPanelPayload } from "./storyVariablesPanelId";
