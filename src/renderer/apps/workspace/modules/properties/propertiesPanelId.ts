/**
 * The property editor's panel id, on its own so a caller that only wants to reveal the rail can name
 * it without importing the panel module — which pulls in every property editor schema behind it.
 */
export const PROPERTIES_PANEL_ID = "narraleaf-studio:properties";
