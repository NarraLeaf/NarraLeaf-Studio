---
title: "task: M5 Dev Mode 纵深 — 变量面板、播放头、调用栈、两层时间线、快照切换"
type: handoff
status: draft
date: 2026-07-23
parent: 2026-07-22-001-feat-story-editor-overhaul.md
---

# task: M5 Dev Mode 纵深

你是执行者。前置阅读：总计划 `2026-07-22-001` §5.6/§5.7 与 §8；`docs/plans/reports/2026-07-23-M4-engine-report.md`（你要消费的引擎 API 签名与已知边缘全在里面）；M3 卡 `2026-07-23-001` §0 纪律与子代理授权、§1 风格铁律**原样适用**。分支：从 develop（`51404f2`+）切 `feat/story-m5-devmode`。报告：`docs/plans/reports/2026-07-23-M5-report.md`（≤60 行同模板）。

**并行提示**：另一执行者在引擎仓做 M4b（text-event token），零文件交集。共享树纪律照旧；**特别注意 `src/shared/types/ipcEvents.ts` 曾长期是另一 session 的 WIP 区**——WI-2 若需加 IPC，动手前确认该文件干净，脏则停机报告。

## 现状地图（2026-07-23 核实）

- Dev Mode 独立窗口 app：`src/renderer/apps/dev-mode/`（`DevModeContent.tsx`：左下浮动工具菜单=跳到下个选择+Blueprint DevTools；`BlueprintRuntimeDebugPanel.tsx`：右滑入面板，tabs=blueprints/output/scope——**本卡新面板全部复用它的 chrome 与 tab 模式**）。
- 共享运行时：`runtime/app/GameApp.tsx` + `GameAppHost.ts`（overlay context 暴露点）；`runtime/app/gameUiSlots.tsx`（`fastForwardToNextChoice` :143、`restoreHistoryInGame` :222、`getHistoryInGame`——engine fastForward/restoreToHistory 的消费现场）。
- 启动链：`DevModeService.launch` → main `DevModeManager.compileAndSendBundle` → `DevModeEntry{kind:"story",storyId,sceneId,startBlockId,snapshotId}` → `GameAppBootAction`。行级冷跳的全部机械已在。
- 编译产物 `compileStudioStoryToNlr` 返回 **`actionIdBindings`**（action↔block 双向映射）——播放头与热跳的关键；核实它在 dev-mode 窗口内可得（编译发生在该窗口）。
- 引擎 API（dist 已含，dev_nomen 已合并 `27eb425`）：
  - `fastForward({until:{actionId}, maxSteps?})` → `{reason:"action"|..., reachedTarget?}`——目标**执行前**停；菜单/栈空/步限=提前停 reachedTarget:false；仅扫根栈。**已知边缘**：目标恰为"进行中 do 块的续延"时可能提前 reachedTarget:true（引擎 M4b WI-0 在修；落地前热跳在容器边界可能早停——按 reachedTarget:false 同路径兜底即可）。
  - `LiveGame.onCurrentActionChange(fc):Token` / `getCurrentActionId()` / 事件 `event:action.current`（每 action 执行前发，含分支——**你要按 actionIdBindings 过滤**）。
  - `LiveGame.getStackSnapshot()`（纯拷贝快照；类型暂用 `ReturnType<LiveGame["getStackSnapshot"]>`，M4b 会补公共导出）。
- 快照（变量覆盖集）：workspace 侧 `story-snapshots/StorySnapshotPanel.tsx`，持久化在 `scenes[].sceneSnapshots`，launch 以 `snapshotId` 播种。
- 场景图：`story-flow/sceneFlowModel.ts` 的 `buildSceneFlowGraph(document)` 纯投影 + `SceneFlowCanvas.tsx`——L1 直接复用，**禁止新建第二个节点图**。

## 工作项

- **WI-0 dist 基线**：确认 Studio `node_modules/narraleaf-react` dist 与合并后 dev_nomen 一致（引擎仓 `build:dev` + postbuild `--target-dir` 惯例重建拷贝一次），Studio 编译器集成测试（`vitest src/renderer/lib/ui-editor/runtime/game`）全绿后再开工。
- **WI-1 故事变量面板**：Dev Mode 面板加 tab（沿用 BlueprintRuntimeDebugPanel 模式）：三作用域实时值读+写（写=engine storable set；GameAppHost/overlay context 加只读+写桥，先例=blueprint scope 的 scopeBridge）。作用域徽标沿用编辑器词汇（Local/Var/Persis）。
- **WI-2 播放头**：订阅 `action.current` → actionIdBindings 反查块 → 当前行。消费两处：①WI-4 时间线的播放头指示；②workspace 编辑器行高亮同步（devMode→main→workspace IPC，先例=devMode console 通道；编辑器侧复用既有跳转/选中视觉，**不抢焦点**、不新发明标记样式）。高频事件节流。
- **WI-3 调用栈 tab**：`getStackSnapshot()` 渲染只读树（根栈+async 分支+loop 配置），随 action.current 节流刷新。
- **WI-4 时间线（L2）**：Dev Mode 面板 tab 列当前场景行序列（bundle 内 story doc 投影：行号+概览文本，复用编辑器的 describeBlock/blockOverview 逻辑所在 shared 层——若其位于 renderer 专属处，用最小投影即可，不搬组件）；播放头=WI-2；点击行：
  - 冷跳（v1 必达）：同窗 relaunch `{startBlockId=行, snapshotId=当前}`（既有 launch 路径）。
  - 热跳（v2，同卡内做）：目标在当前执行点**之后** → `fastForward({until:{actionId}})`（blockId→首 actionId 经 bindings）；`reachedTarget:false` 或目标在前 → 自动回落冷跳（静默回落，不弹任何提示）。
- **WI-5 场景图（L1）**：同面板加 tab 嵌只读 SceneFlow（`buildSceneFlowGraph` + Canvas 复用，devMode 只读态）：当前场景高亮，双击节点=从该场景顶部 launch。
- **WI-6 快照切换**：面板内快照下拉（bundle 携带的 sceneSnapshots），切换=同 `startBlockId` relaunch 新 `snapshotId`。

顺序：WI-0 → 1 → 2 → 3 → 4 → 5 → 6；WI-5/6 可按裁剪规则顺延（报告注明）。

## 验证与停机

lint/vitest 同前（新失败 0）；真机：Dev Mode 本身可跑，逐 WI 自验+截图（变量写入生效、播放头随执行走、热跳停在目标行前、回落路径、调用栈含并行分支）；用户手测欠账继续累积由用户择时清。停机条款沿用 + 特化：引擎 API 不够用（**不许在本卡内改引擎**，报告给 orchestrator 协调 M4b）；ipcEvents.ts 脏；bundle 内拿不到 actionIdBindings（架构核实失败）。
