---
title: "report: 引擎 0.16.1 跟进 — Push 信箱盒修复 + 导出 Darkness"
type: report
status: done
date: 2026-07-24
parent: 2026-07-24-002-task-engine-0-16-1.md
---

# 引擎 0.16.1 跟进报告（WI-1 / WI-2）

工作仓 = 引擎 `narraleaf-react`。隔离 worktree `D:/Dev/org/NarraLeaf/nlr-wt-push-darkness`（node_modules junction 自主检出），分支 `fix/push-letterbox-and-darkness` off `dev_nomen 4a31d1e`。单提交 `849d08c publish: 0.16.1`。**未发布、未打 tag、未 push** —— 可发布。

## WI-1 Push 信箱盒溢出 — **done**

`push.ts`：平移单位 `vw/vh` → `%`（`axisUnit`→`axisSign`，去掉 unit 维度；`translate` 用 `${offset}%`）。语义与 Studio 保留的 `Slide` 逐字一致：`100%` 相对转场层 wrapper（`inset:0` 信箱盒）自身量取，非窗口，非设计比时不再溢出露背景。纯内部单位换算，**公开 API 签名零变**（`PushOptions` 不动，`translate` 仍是 identity-at-rest）。JSDoc 补了「为何是 % 而非视口单位」的信箱盒原理。

## WI-2 导出 Darkness — **done**

`common/transition.ts` barrel 加 `Darkness` + `DarknessOptions`（值 + 类型）。`darkness.ts` JSDoc 改写：去掉已失真的「Internal」措辞，补亮度压暗语义（把入图亮度从 `1-from` 动到 `1-to`，同时弃出图；它就是 `image.darken(amount,duration)` 的底座）。**行为零改**。与 0.16.0 Reveal/Mask 共存无冲突：独立 `ImageTransition` 子类，无共享静态词表，全量 tsc + settled-pose 枚举测试（含 Darkness/Reveal/Mask）通过为证。

## 测试

`builtinTransitions.test.ts`：Darkness 从深路径 import 改走 barrel（`narraleaf-react`），删陈旧「未导出」注释；Push 断言 `vw/vh`→`%` 并从单例扩到四方向 × enter/exit（prev/target 两相）+ 显式 `not.toMatch(/vw|vh/)`；`settled pose` 注释同步为 `%`；新增 Darkness 构造+参数+copy 冒烟。

## 验证与停机

- `vitest run` 全量：**316 通过 / 0 失败**（310 基线 + 6 新：Push +3、Darkness +3）。
- `tsc --noEmit`：exit 0。`eslint`（全 src，CRLF 已 `--fix` 归一）：exit 0。
- 停机项：**无**。WI-1 未触公开签名（预期内）；WI-2 导出未暴露自身缺陷、未改行为。

## 交用户 / orchestrator

1. 归用户：worktree 内 `npm publish` + `git tag v0.16.1`（package.json 已 0.16.1、CHANGELOG 已补 `## [0.16.1]`）。
2. 0.16.1 上 npm 后，Studio 侧采纳（删 `Slide`、`slide`→native `Push`、接通 `darkness`）由 orchestrator 出微卡 —— 不在本卡。
3. 本报告未提交（Studio 主检出为共享 checkout，留 orchestrator 处置）。
