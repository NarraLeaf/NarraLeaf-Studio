# M2 报告
分支/commits: feat/story-m2-operation-layer @ 361734b..dd34006（5 commits，基于 develop 8129e16）

## 状态
WI-0 尾款(差分头像+P1+P2×9): done · WI-1 检查器迁右栏: done · WI-2 quickParams: done · WI-3 右键菜单+禁用(schema v7): done · WI-4 语音试听+检查器语音区: done

## 文件
- WI-0: scene-editor/storyBadgeImageCache.ts(新·id 键共享 URL/裁剪缓存), StorySceneEditorRows.tsx(BlockBadge/GroupExpressionMember 走 useBadgeImageUrl), shared/utils/characterVariant.ts(resolveVariantEntry), characters/editors/components/HeadThumbnail.tsx, storySceneBlockUtils.ts(annotateDialogueGroups parentId/getCharacterColor+对比度), useStorySceneEditorController.ts(过滤选区裁剪/revealBlock/Backspace), CharacterEditor+PreviewPanel(form 级 portrait 入口), styles.css(插槽 46px), i18n characters
- WI-1: storyInspectorBridge.ts(新), StoryInspectorPanel.tsx(新), StorySceneEditorTab.tsx(注册第4面板+桥), StorySceneActionInspector.tsx(去卡片壳), StorySceneEditorRows.tsx(删行内展开), i18n story
- WI-2: storyQuickParams.tsx(新)+.test, StorySceneEditorRows.tsx, commands/spec.ts(quickParams 声明)+specs/{scene,character,sound}.ts, i18n story
- WI-3: shared/types/story/document.ts(disabled+v7), storyModel.ts(isBlockDisabled/collectInvalidBlocks/迁移), StoryService.setBlockDisabled, storyCompiler.ts(compileBlock/choice/condition 跳过), controller(startInsertBefore/toggleDisableSelection), StorySceneEditorTab.tsx(ContextMenu+插上方), rows(禁用弱化+onContextMenu), tests×3, i18n story
- WI-4: voiceAudition.ts(新·单例播放器), useStoryVoiceState.ts(新·抽出共享 take 查询), StoryVoiceIndicator.tsx(试听键+mic), StorySceneActionInspector.tsx(VoiceInspectorSection), i18n storyInspector/story

## WI-0 · P2 九条
1 空对话行 Backspace 过滤态锚点 → done（锚点须可见，否则回退 delete-and-step-back）
2 过滤选区残留隐藏行 → done（narrativeOnly 时 effect 把选区/active 裁剪到可见）
3 revealBlock/搜索跳到隐藏行 → done（跳转赢过滤，命中隐藏行时自动关过滤）
4 分组跨容器边界 → done（annotateDialogueGroups 要求同 parentId；补 3 单测）
5 名牌对比度+让位选中 → done（相对亮度带 0.03–0.85 守卫回退 ink；selected→suppressColor）
6 form 级 portrait 覆盖不可新建 → done（选“最小入口”：预览头 hover Layers 切换建/清 form 覆盖）
7 舒适密度插槽 min-h 35↔46 → done（data-story-insert-slot + comfortable CSS 46px）
8 HeadThumbnail frame 有→无永久 opacity-0 → done（crop 改从缓存实时读，state 仅触发重渲染）
9 补测试 → done（断组×3、filter→group 行号、对比度、disabled 编译×2、v7 迁移、build 门、buildVisibleRows 传播；共 +18）

## 偏离与决策
- WI-1: 检查器随选中行“切换”＝按激活切换（Enter/双击/右键“打开检查器”）＋ Escape 关（行保持选中），非点击即跟随——1:1 复刻行内旧行为、不碰 editorMode 契约。对话/旁白检查器经右键“打开检查器”可达（hasInspector 为真，绕过文本编辑路径）。
- WI-2: 无 block→args 解析器，getQuickParams 直读 payload；spec.quickParams 为声明/意图。boolean(loop) 原位翻转不开 popover；转场“种类”(`t=` 枚举)留检查器，仅暴露时长(`d=`)——均记为 §6 判断。
- WI-4: 全局单播覆盖故事编辑器全部试听面（行+检查器）；voice 表保留自有播放器，不耦合两编辑器音频生命周期（§6 边界）。demo3 无配音 locale，故试听键按设计隐藏。

## i18n（en+zh 同加）
characters.preview.portraitFormScoped{On,Off}; story.sceneEditor.inspectorPanel; story.quickParam.{waitLabel,jumpLabel}; story.rowMenu.*(8); story.rows.voice{Play,Stop}; storyInspector.section.voice + storyInspector.voice.{voiced,none,stale,openTable}

## 验证
- lint: `yarn lint` 全绿（5 tsc project，exit 0）。
- vitest: 全量 1920 通过 / 8 失败＝win32 基线原样（runtimeProtocol×2, shared/utils/path×3, storageManager, GameBuildManager darwin, mobileSigningIdentity；均 POSIX/权限，非我引入），新失败 0；新增 18 例全过。
- 真机（yarn dev 隔离实例·userData-dev·CDP 9222·Demo/demo3）: WI-0 角色/差分行渲染 `<img>` 头像（bare speaker Youk 走图标兜底，正确）；WI-1 检查器在右栏联动、行内展开已删；WI-2 `@wait 0.5` → “Wait 0.5s” token + 预设 popover(0.2/0.5/1/2/3s)；WI-3 右键菜单全 7 条、禁用行 opacity-45 弱化；加/禁/删净零已还原 demo3(9 行)。截图见 assets/2026-07-23-M2-0{1..5}.png。
- Dev Mode 禁用编译跳过: 由 storyCompiler.integration 单测权威覆盖（disabled 出块+子树跳过+0 诊断），未另起 Dev Mode 冷启动（需快照、脆弱面高）。

## 风险与已知
- WI-4 语音试听/检查器语音区未截图（demo3 无配音 take，按钮按设计隐藏）——逻辑复用已验证的 StoryVoiceIndicator take 查询 + voice 表 audition 范式。
- 真机验证在共享树跑，期间另一 session 新增了 hooks/index.ts+useProjectDisplayName.ts（非我，未纳入我的 commit）。为验证打开 demo3 触发 v6→v7 迁移落盘（加性、块不变）。

## 需要 orchestrator 重点验收
1. WI-0 差分头像硬验收：用文件侧种子（角色配 form+variant+立绘）复验 `/show`+对话行头像按 portrait/自动取景裁剪（我这边 demo3 已见项目资产 `<img>` 加载，但未逐帧核实是差分 vs 缩略）。
2. WI-1 “检查器随选中行切换”取舍：按激活切换 + Escape 关，是否符合期望（对比 §2 WI-1 措辞）。
3. WI-2 `t=` 转场种类留检查器、boolean 原位翻转 的两处 §6 判断。
