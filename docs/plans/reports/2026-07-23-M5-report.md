# M5 Dev Mode 纵深 报告（NarraLeaf-Studio）
分支 `feat/story-m5-devmode`（从 develop `4186d98` 切），3 commits `5b3db1b..1e09417`，未合并未 push。

## 状态
WI-0 dist 基线 · WI-1 变量面板 · WI-2 播放头(①面板时间线+②工作区行高亮) · WI-3 调用栈 · WI-4 时间线(冷跳+热跳) · WI-5 场景图 L1 · WI-6 快照切换 — **全 7 项 done，无裁剪**。

## 停机条款核查
- `ipcEvents.ts` 动手前 git 干净 → 未触发停机；新增双通道照 `devModeForwardBlueprintDebugEvent` 先例。
- bundle 内 `actionIdBindings` 可得（编译在 dev-mode 窗口，核实通过）；引擎 API 够用，未改引擎。
- **观察（非本卡）**：并行 M4b 的报告 `docs/plans/reports/2026-07-23-M4b-engine-report.md` 已 untracked 落入共享树；未触碰未提交（逐文件 `git add`）。

## 文件 / 关键设计
- 桥 `GameAppHost.ts` `GameAppStoryRuntimeBridge`（overlay context 暴露，先例=fastForwardToNextChoice/scopeBridge）；`GameApp.tsx` 全 ref-backed 稳定 useMemo，播放头 token 每 session 重绑、稳定 listener 集跨 relaunch 存活。
- 编译器 `storyCompiler.ts`：`CompiledNlrStory` 增 `savedNamespaceName`+`sceneLocalNamespaceNames`（`DevTools.getNamespaceName`，不依赖引擎前缀约定），三处 return 全填。
- 面板 `StoryRuntimeDebugPanel.tsx`（复用 BlueprintRuntimeDebugPanel chrome+tab；变量/栈/时间线/场景 4 tab + 快照下拉）；`storyRuntimeDebugModel.ts`(+test) 纯投影（行序列/反查/变量清单，最小 describeBlock 投影，不搬编辑器组件）；`DevModeContent.tsx` 第二菜单项+互斥面板+播放头转发。
- WI-2②：`interface.ts/renderer.ts/devMode.ts/ipcEvents.ts` 通道对；`devModeAction.ts` 主进程 handler（校验窗口+项目，不 show/focus）；`StoryRowHighlightBridge`+`storyRowHighlightBus`+`StorySceneEditorTab` 订阅。
- WI-5：`SceneFlowCanvas` 加 `currentSceneId` prop、`SceneFlowNode` 当前场景 ring（均 additive optional，工作区用法不变）。

## 变量作用域运行时（核实）
Local=scene→Storable ns `local:<runtimeName>`（仅活动场景存在）；Var=saved→ns `persistent:__nlr_story_saved__`；Persis=persistent→scopeBridge（有 subscribePersistence）。引擎无 storable 变更事件 → 随 `action.current`+写后刷新。变量/时间线/场景三 tab 均按 block→scene 跟随运行场景。

## 偏离与决策
- 冷跳=同窗 `startStoryInGame({forceReinit})`（既有入口路径，非重建 OS 窗口），符合"同窗 relaunch"。
- 三 tab 跟随运行场景（跨 jump 播放头始终可见），非固定入口场景——更贴合"播放头随执行走"。
- 工作区高亮用 `selectRow`（不翻 narrativeOnly 过滤器）+仅当行已显示(DOM 存在)+不 focusRoot，最不打扰（对抗审 med 项修正）。

## i18n
`devMode.tabs.{variables,stack,timeline,scene}`+`devMode.runtime.*` en+zh 双份，结构对齐（zh `satisfies LocaleNamespace`）。作用域用命令词汇 Local/Var/Persis。

## 验证
- `yarn lint`（tsc）exit 0 全绿。
- vitest：编译器集成 102/102（WI-0 闸门）、新模型 test 5、dev-mode/app/story-flow/import-boundary 全过，**新失败 0**。win32 基线 `runtimeProtocol` 2 失败为既有盘符归一化漂移（非本改动）。
- 子代理对抗审 diff（本卡未拆主线给子代理，仅用于验收审查）：无 high；两 med（revealBlock 翻过滤器、变量 tab 固定入口场景）已修，另修一处我引入的 bug（转发用入口 sceneId 而非块所属场景）。

## 风险与已知
- 真机视觉验证未由我驱动（按 orchestrator-lean-context 分工，orchestrator 不亲手驱动），未产出截图，累积为**用户手测欠账**。
- 热跳容器边界早停（M4b WI-0 落地前）按 `reachedTarget:false` 回落冷跳兜底。
- relaunch 后转发去重 ref 跨会话保留 → 极端"同块首帧"可能漏一次高亮（无害，行已高亮）。

## 重点验收（用户手测，2-3 处）
1. 变量写入（数值/布尔/JSON）后引擎重评生效；非活动场景 Local 灰显只读。
2. 播放头随执行走：面板时间线 ▶ + 工作区行高亮同步，不抢焦点、不翻过滤器。
3. 热跳停在目标行前；越过/不可达/跨场景静默回落冷跳；调用栈含并行分支+loop 配置。
