# M3.1 报告
分支/commits: feat/story-m3-1-command-tail @ 11ec226..f0597ae（8 commits：WI-0/1/2a/2b/2c/3 + 评审修复，基于 develop 114f130）。未合并未 push。

## 状态
WI-0 复核 nits(3): done · WI-1 声明 ghost 确认: done · WI-2 palette(2a 分类浏览 / 2b 拼音 / 2c 指令手册): done(全三项，未裁剪) · WI-3 组头站位下拉: done · WI-4 注册表迁移: **顺延**（门通过，见下）

## 文件
- WI-0: StorySceneEditorTab.tsx(检查器桥经 editorRef 取最新 editor→杀陈旧 undo；republish 门 JSON.stringify→payload 引用比较), useStorySceneEditorController.ts(离视关槽纳入 replaceBlockId), storyBadgeImageCache.ts(无退订设计前提注释)
- WI-1: declarations.ts(describeDeclaration 抽出)+.test, storySceneBlockUtils.ts(describeBlock 复用), storySceneEditorTypes.ts(insert mode `confirmation?`), controller(声明提交携带 ghost 收据 + 首次编辑清除), StorySceneEditorRows.tsx(CommandGhostHint 收据分支 + 空槽 placeholder 抑制)
- WI-2a: StorySceneEditorRows.tsx(useActionCommandMenuState/ActionCommandMenu 分类浏览 vs 扁平过滤); 2b: scripts/gen-story-command-pinyin.mjs(一次性生成器,内嵌字典,零依赖) + storyCommandPinyin.generated.ts(45 条入库) + storyCommandSearch.ts(拼音入 keyword)+.test; 2c: storyCommandManualModel.ts(+.test) + StoryCommandManual.tsx(spec 派生手册,挂 scene-editor 头栏 BookOpen)
- WI-3: storySceneEditorTypes.ts(StoryStagePlacement + appearance position/positionSourceId/shown), storySceneBlockUtils.ts(buildDialogueAppearances 累积站位)+.test, controller(setDialogueGroupPosition 改写/插入), StorySceneEditorRows.tsx(GroupHeadPositionControl), StorySceneEditorTab.tsx(接线) + i18n position
- 评审修复: StoryService.ts(updateDeclaration 改为重赋 payload)

## 复核 nits（逐条）
1 检查器陈旧 undo（桥回调经 editorRef 取最新 scene；顺带 payload 引用门替 JSON.stringify）: done
2 replace 槽离视关槽（anchor 纳入 replaceBlockId）: done — 现状唯一重开路径已经 target.beforeBlockId=replaceBlockId，本改硬化不变性
3 ensureAssetInvalidationWired 无退订: 注释说明设计前提（单窗单项目），未改

## 偏离与决策
- WI-2c 帮助入口: 现有帮助入口（键盘手册 shift+?）在状态栏（共享树 WIP 区），改挂 scene-editor 自有头栏 BookOpen 按钮，避开他人 WIP。手册为只读浮层，复用手册同款 chrome。
- WI-1 收据落点: 携于 insert EditorMode（随 chooserDismissed 同类一次性清除），不另设 state，天然限定于声明后那一个槽。
- §0 子代理: 3 个只读 Explore（帮助入口勘查 + WI-4 爆炸半径测绘）+ 1 个对抗式 diff 评审。评审报 3 缺陷（WI-3 站位往返对未 show 说话人失效并叠重复 /move、WI-3 无站位 move 清零站位、WI-0 引用门漏声明就地改）——全部逐行复核并修（commit f0597ae），加 3 回归测试。未行使 WI-2/3 裁剪权（全做）。

## WI-4 现状核对与顺延裁决
启动门 **通过**：src/main、蓝图模块、project/app 工作树全干净（唯一未跟踪是并行 M4-engine session 的报告文档，在触碰区外）。**仍整体顺延**：实测爆炸半径为里程碑级（bible M4）——~27 生产文件 + ~19 测试文件、新建 VariableRegistryService、新增项目数据文件、story v7→v8 + 蓝图 v8→v9 双 schema 迁移、蓝图运行时(Dispatcher×7/Evaluator/executors)改读。属"干净切换无双读"（bible §3.1）→ 不可部分启动（卡 §"不要部分启动"）。另存 bible 内部张力：§3.1 注册表"持有全部 persistent" vs §3.4b「/persis 为声明行、仅蓝图声明的 persistent 不以行呈现」——注册表究竟接管哪一半未定。按 §8 停机条款（规模超预估 + bible 与现状冲突无法判真），顺延至专属 M4-变量里程碑任务，未动一行触碰区。

## i18n
新增 story.commandManual.*（open/title/searchPlaceholder/aliases/empty）与 story.position.*（label/left/center/right），en+zh 两份。编译器诊断沿旧英文约定。

## 验证
- yarn lint (tsc 5 project): 全绿 exit 0。
- vitest 全量: 1949 通过 / 8 失败 = win32 基线原样（path×3, runtimeProtocol×2, storageManager, GameBuildManager, mobileSigningIdentity；均 POSIX/权限），新失败 0；新增测试全过（describeDeclaration×3, 拼音×3, 手册派生/过滤×7, 站位累积×6）。
- 真机视觉: 按既定验收分工（子代理审 diff 已做，用户手测视觉）顺延用户手测——见重点验收；逻辑由 tsc + 单测 + 对抗评审覆盖。

## 风险与已知
- WI-2c 拼音表随 zh 指令 label 静态生成：改 label 需重跑 `node scripts/gen-story-command-pinyin.mjs`（未挂 package.json，避开共享树）。
- WI-3 站位对未 show 说话人插入 /move（依卡面），运行时 move 隐藏角色为 no-op；编辑器不再为其造头像（shown 门）。
- WI-4 与 M2 顺延手测（差分头像、M2 五面）仍在用户欠账。

## 重点验收（真机，用户手测）
1. WI-1: `/var gold 0` Enter → 声明落行，下方新槽 ghost 区短暂 `✓ Var gold: number = 0`，下一次输入即消隐（不弹 toast）。
2. WI-2: `/` 空态按类别分组浏览（色+图标头）；输入切扁平排序；拼音 `beijing`/`bj` 命中背景；头栏 BookOpen 开指令手册（签名/别名/说明，可搜）。
3. WI-3: 对话组头 hover 出站位下拉（左/中/右）；改写会重写最近 /show|/move 的 at=（无则组头上方插 /move），可 Ctrl+Z；文档始终是命令行。
