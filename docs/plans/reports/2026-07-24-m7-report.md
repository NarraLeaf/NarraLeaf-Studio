# M7 演出透镜 报告（NarraLeaf-Studio）
分支 `feat/story-m7-staging-lens`（从 develop `2cdc6fd` 切，含 NLR 0.16.0 迁移），未合并未 push。

## 状态
WI-1 透镜渲染 + header 切换: done · WI-2 行语义保持: done（dnd 原生复用，无降级）· WI-3 编辑便利（尾部 + / 模式徽标）: done。纯投影，零新文档字段，未碰引擎/transitions 词表。

## 停机条款核查
- `Control.any` 败者不掐断（2026-07-23 裁决）：透镜**不暗示"胜出即中止"**——race 只在最早已知完成点画一根细标记，所有条形仍绘满全长（败者继续跑）。`projectStagingLens.winnerFinishMs` = 已知+启用轨的 min，`LensBar` 不截断任何条。未触发"需改交互契约"或"dnd 成本触发降级"两条停机。
- 共享树：`git status` 里 `docs/plans/reports/2026-07-24-engine-0-16-1-report.md` 为并发引擎会话产物，未触碰；逐文件 `git add`，禁 `-A`。

## 文件 / 关键设计
- `storyStagingLens.ts`(新,+test): 纯投影。`deriveBlockTiming`/`actionDurationMs`（durationMs / transition·transform.durationMs / fadeMs / screenEffect dur+hold / wait→delay / dialogue pauseAfter；无来源=unknown）；`blockFinishMs`（sequence=Σ、parallel/race=max、repeat×times）；`projectStagingLens`（scaleMs=已知启用轨 max 落地 1，race winnerFinishMs=min）；`resolveEffectiveLensContainers`（去 stale/非 parallel-race/被外层透镜嵌套者）；`applyStagingLensToRows`（行投影：直子标 track、更深后代裁剪、嵌套容器=单 subgroup 轨）。
- 渲染 `StorySceneEditorRows.tsx`: `LensToggle`（hover-reveal 列表⇄时间线，开态常显 GanttChart/List）；`ContainerModeBadge`（all/allAsync/any，control 类别色）；`LensTrackContent`（徽标+`BlockPreview` 概览含 `d=` 快编 + 右侧 `LensBar`）；`LensBar`（已知=类别色比例条、wait=斜纹延迟、未知=等宽虚条、race=最早完成细标记）；`LensTailAdd`（尾部 + 复用 `onAddInside`→InsertRow inside）。用独立 `translate`/非 transform 定位（Tailwind v4 冲突）。
- 状态 `storyEditorSessionStore.ts`: 按容器 id 持久化透镜开关（PanelStateService，沿 M1 密度存法，key `story:editor:lens-state`）。控制器 `lensContainerIds` React 态镜像 + `toggleContainerLens`；`visibleRows` useMemo 强展开透镜容器、跑 `applyStagingLensToRows`（dep 加 lensContainerIds）。
- 行语义保持：轨仍是 `visibleRows` 中的真行——外层行 div 的 onClick/onContextMenu/onDoubleClick/useSortable(grip 在 col2)/`data-story-row-block-id` 全不变，只换 col3 内容。选择/检查器/右键/dnd/播放头(selectRow)/行号原生沿用，无降级。

## 偏离与决策
- 透镜=行的"渲染模式"而非抽走子树：直子块留在 visibleRows（保住全部行机械），只裁剪更深后代 + 换内容列渲染。比"控制器换单行/header 内画全部"更省、语义 1:1。
- 嵌套双透镜：外层开时内层降为 subgroup 轨（`resolveEffectiveLensContainers` 去嵌套），避免重复/越权渲染。
- subgroup 轨"点击进入"取"跳到该行"（选中，默认行为）；双击=card-less 容器 startInsertInside。留待反馈再议展开语义。
- 模式徽标为 WI-3 明文准许项（非新发明 pill）；用既有 control 类别色 + 边框，克制。

## i18n
`story.lens.{toLens,toList}` en+zh 双份（切换按钮 title）。模式徽标 all/allAsync/any 为引擎规范词（英文，同命令词，不译）。无其他新用户文案。

## 验证
- `yarn lint`（tsc 5 project）exit 0 全绿；`build:apps:dev` 6 app 全 bundle 成功。
- vitest 全量：8 失败 = win32 既有基线原样（path×3 / runtimeProtocol×2 / storageManager / GameBuildManager / mobileSigningIdentity，均 POSIX/权限），**新失败 0**；新增透镜单测 14（时长/延迟/未知/race/嵌套/disabled/行投影/裁剪/**畸形图循环守卫**）全过；scene-editor 套件 268 全过。
- 子代理对抗式审 diff：无 HIGH、无硬约束违背。5 项发现，3 修 2 收：**修**①投影三处图遍历补循环守卫（畸形 parentId/childrenIds 环不再挂 UI 线程）；②`narrativeOnly` 开时透镜整体停用（否则并行内的对白子块会成孤儿轨）；③disabled 轨改画等宽虚条（不参与刻度，避免被 clamp 成满宽误导）。**收**④轨上 hover"从此行播放"按钮让位给时间条，仍在右键菜单可达（有意，行仍全功能）；⑤失效容器的持久化条目不清理（读取侧忽略，无渲染 bug，同 view-state 陈旧 sceneId 处置）。
- 真机视觉/交互：**未由我驱动**（沿 orchestrator-lean-context 分工 + M5 先例），累积为用户手测欠账（见重点验收）。

## 风险与已知
- 真机截图欠账（构造并行演出、点轨、快编、Dev Mode 对齐）落用户手测。
- 透镜时长为静态投影：sound 无 fade / setVariable 等无时长动作按 unknown（等宽虚条）呈现，属预期（"无时长按未知"）。
- 收项⑤：持久化透镜 map 不主动裁剪陈旧条目（inert，无 bug）。

## 重点验收（用户手测）
1. 构造 3-4 元素并行（/move + /show 淡入 + /bgm + /wait），header hover 出时间线切换；开后每直子一条轨，比例/斜纹延迟/等宽虚条正确。
2. 点轨=选中该行 + 右栏检查器（Enter/双击）联动；右键菜单、`d=` token 原位快编、grip 拖拽重排、行号、播放头高亮均如常。
3. race 容器：最早完成处细标记、其余条形绘满全长（不截断）；模式徽标 any。尾部 + 插入子块。Dev Mode 播放与透镜时序一致。
