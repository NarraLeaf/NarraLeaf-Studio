# M3 报告
分支/commits: feat/story-m3-command-layer @ 1880c9e..4e1a8d6（4 commits，基于 develop 493ec04）

## 状态
WI-0 尾款(9): done · WI-1 交互修订: done（ghost 确认顺延） · WI-2 概览投影: done · WI-3 编译器修复(c/d/e): done · WI-3 注册表迁移(§3.1/3.2) + WI-4/5: 顺延 M3.1

## 文件
- WI-0: storyQuickParams.tsx(+.test), storyBadgeImageCache.ts(重试+资产失效), StorySceneEditorTab.tsx(检查器 re-show/republish 门控 + 试听 teardown), useStorySceneEditorController.ts(插槽离视关槽 + 删 statusText 死态), useStorySceneClipboardHandlers.ts, declarations.ts(+.test)
- WI-1: commandTrigger.ts(insertChooserType), storySceneEditorTypes.ts(删 chooser 存储), controller(删 chooser sets/startLineEdit 不压制), StorySceneEditorRows.tsx(派生 chooser)
- WI-2: storyQuickParams.tsx(blockOverview/BlockOverview/OverviewFragment)+.test, StorySceneEditorRows.tsx(单一投影路径)
- WI-3: storyCompiler.ts(collectPersistentKeys+4 处引用校验+结构相等+场景重名), storyExpressionEval.ts(export strictEquals)+.test, storyCompiler.integration.test(+2)

## 尾款九条（逐条）
1 quickParam 标签 t→d（对齐 bg spec quickParams=["d"]）: done
2 检查器 dead-Enter（改按 blockId re-show，手动隐藏后 inspector→inspector 重现）: done
3 插槽隐形（锚点行离 visibleRows 即关槽）: done
4 badge 失败重试（失败条目下次订阅重试）: done
5 badge 资产失效（订阅 Assets updated/deleted 失效 project 条目）: done
6 试听 teardown（tab deactivate/unmount 即 stop）: done
7 检查器桥逐键重发布（按 inspected block 签名门控 republish）: done
8 禁用声明语义（declarations.ts 注释：禁用仍声明+播种；+断言测试）: done
9 statusText 硬编码（选删除 write-only 死态 + 连带清理 restoreHistoryState label / describeBlock import / insertBlock deps）: done
另两项顺延人工验收（差分头像视觉、M2 五面手测）→ 见重点验收/风险，落用户手测。

## bible M4 现状核对
已在: (a) Local 播种@scene 头、(b) persistent 读回落声明默认、声明即行 v6 + declarations.ts 覆盖三作用域(含 persistent)、schema=v7、blueprint schema=v8。
未在→本次建: (c) persistent 引用编译校验(赋值/表达式赋值/条件/插值 4 处)、(d) persistent 条件结构相等、(e) 场景重名诊断。
未建→顺延 M3.1: VariableRegistryService(§3.1) 不存在；BlueprintDocument.persistentVariables 仍在（~20+ 消费者：蓝图节点/成员树/编译/运行时/stats/搜索）；StoryVariableRef persistent 臂仍 storageKey(§3.2 未统一)。

## 偏离与决策
- WI-2: 未在 spec 加 overview()——committed 块无 command id(B11) + 泛型动词 payload→spec 多对一，改按 payload 形投影(blockOverview)，与 describeBlock/getBlockBadgeInfo/getQuickParams 同形；describeBlock 降为回落。
- WI-1 ghost "✓" 确认: 顺延（bible §8 标"已基本闭环"，落行即反馈）；chooserDismissed 保留为一次性 Escape 位（不可 derive，已是 one-shot）。
- §4 裁剪: WI-3 注册表迁移 = 里程碑级跨仓改动（src/main + 蓝图，共享树 WIP 区），超预估 → 顺延 M3.1，仅落自足编译器修复；WI-4/5 同顺延。
- WI-3(c) 更严: 无声明的 persistent 引用现 warn+skip；一处本地化测试 fixture 补 persistent 声明（对齐 bible，非弱化）。
- §0 子代理: 1 个，做尾款#9（删 statusText 跨 2 文件），diff 逐行复核 + 连带清理已核实，tsc/全量测试佐证。

## i18n
无新增用户文案。编译器诊断沿用既有纯英文约定（与 scene/saved 同族诊断一致，非 i18n 面）。

## 验证
- yarn lint (tsc 5 project): 全绿 exit 0。
- vitest 全量: 1930 通过 / 8 失败 = win32 基线原样（path×3, runtimeProtocol×2, storageManager, GameBuildManager, mobileSigningIdentity；均 POSIX/权限），新失败 0；新增测试全过（declarations×2, blockOverview×3, strictEquals×3, persistent 校验/场景重名×2, quickParam 标签修正）。
- 真机视觉: 顺延用户手测（见重点验收），逻辑由 tsc + 单测覆盖。

## 风险与已知
- WI-3(c) 严格化：无声明的 persistent 引用被诊断（bible 要求）。若存在"host 注入但 Studio 未声明"的 persistent 用法会被 warn+skip——请确认无此模式，否则补声明或放宽校验。
- ghost 确认 / WI-4 palette / WI-5 站位 / 注册表迁移均顺延 M3.1。

## 重点验收（真机，2-3 处）
1. WI-1 草稿行重开：残缺输入/命令行 Enter 落琥珀草稿行；点回草稿行 → 补全菜单立即打开（非空需先输入）；Escape 一次性（下键即恢复）。
2. WI-2 概览行：代表指令 [徽标][目标][token] 单流（setBgm vol/loop、character enter 的 d、wait/jump），token 内联非叠加；setBackground/transform 富预览不变。
3. WI-3 + M2 顺延：persistent 声明/引用/未声明诊断 + Dev Mode 播种一致；差分头像（对话/表情行显立绘取景）+ M2 五面（检查器/快编/右键/禁用/试听）快速过面。
