# M1 报告
分支/commits: feat/story-m1-reading-layer @ 6a1299a..b5fbf86（7 commits，基于 develop 3aeef2d）

## 状态
WI-1 颜色: done · WI-2 行类视觉: done · WI-3 差分头像/portrait: done · WI-4 裁剪编辑: done · WI-5 分组: done · WI-6 密度/过滤: done

## 文件
- WI-1: character/types.ts, CharacterProfile.ts, characters/editors/CharacterPropertiesEditor.tsx, scene-editor/storySceneBlockUtils.ts(getCharacterColor), StorySceneEditorRows.tsx(名牌), i18n characters en/zh
- WI-2: StorySceneEditorRows.tsx(左缘条+徽章底色), storySceneBlockUtils.ts(describeBlock charOp), i18n story en/zh
- WI-3: shared/utils/characterVariant.ts(新), ui-editor/runtime/game/storyCompiler.ts, character/{types,CharacterProfile,CharacterAppearance}.ts, characters/editors/{CharacterEditor(cloneForms),components/HeadThumbnail}.tsx, scene-editor/{storySceneEditorTypes,storySceneBlockUtils,StorySceneEditorRows(BlockBadge),useStorySceneEditorController}.ts(x)
- WI-4: characters/editors/{CharacterEditor,components/PreviewPanel}.tsx, i18n characters en/zh
- WI-5: scene-editor/{storySceneEditorTypes,storySceneBlockUtils,useStorySceneEditorController,StorySceneEditorRows}.ts(x) + storySceneBlockUtils.test.ts
- WI-6: scene-editor/{storyEditorSessionStore,storySceneBlockUtils,useStorySceneEditorController,storyEditorTextStyle}.ts(x), StorySceneEditorTab.tsx, styles/styles.css, i18n story en/zh

## 偏离与决策
- WI-2B（摘要 label）: 无 block→command 解析器，且泛型动词命令与 operation 非 1:1；不建解析器（违"不重写摘要体系"）。改为本地化 character operation 动词（story.describe.charOp.*），交付相同"本地化 label+关键参数"，范围限最刺眼的角色动作行。
- WI-4: 复用既有 ImageCropper 模态（§1"复用现有覆盖层/手柄惯例"），由预览上的 hover-reveal 键触发，而非在缩放/平移预览上叠内联矩形（内联跟踪成本会触 §8 翻倍）。"未覆盖时编辑 profile 级 rect"照做；**从最小 UI 无法新建 form 级覆盖**（编辑落到 profile；form 级在数据/渲染层已支持并优先解析）。另修：ImageCropper 默认框忽略 aspect 锁→改为播种正方形默认（b5fbf86）。
- WI-5: 组内文本缩进用固定值 GROUP_MEMBER_INDENT(pl-6)，非逐行像素对齐名牌（名牌宽不可测）；导轨线 bg-edge。组内对话行读/编两态都隐藏名牌（避免 VS Code 跳动）→ 成员行说话人不可行内改。
- WI-3: 变体组级 portrait 覆盖按卡允许下沉到 form 级为止；profile+form 两级已实现。

## i18n
characters.properties.color; characters.preview.{setPortrait,resetPortrait,portraitTitle}; story.describe.charOp.{enter,move,exit,expression}; story.view.{narrativeOnly,comfortableDensity}（均 en+zh）

## 验证
- lint: 全绿（5 个 tsc project 全过）。
- vitest: story/character 范围 363 通过，**新失败 0**；新增 7 例（分组/外观/过滤规则）；编译器 53 例全过=行为零变化。基线 win32 失败 **8**：runtimeProtocol×2, shared/utils/path×3, storageManager, GameBuildManager(darwin), mobileSigningIdentity（均 POSIX/darwin/权限，非我引入）。
- 应用实测（真机 Demo 项目 · CDP 驱动，项目留原样）: 启动+故事编辑器渲染无崩溃；WI-1 颜色字段渲染✔（无对话行截色）；WI-2 类别条 DOM 核实（声明/setVar→data #b8aa86，jump→scene #8fa9c7）；WI-3 角色编辑器渲染 Narra 立绘+抽取共享；WI-4 裁剪器在真立绘上开+9 手柄，修后默认 1956²正方；WI-6 密度 attr+min-h 35↔46px 生效、过滤 3→0 行、均持久化；Dev Mode 编译+启动到菜单/场景无错（WI-3 红线过）。
- 截图: docs/plans/reports/assets/ 02-category-rows / 03-character-editor / 04-portrait-cropper / 05-devmode / 06-density-comfortable .png
- **未截图**: WI-5 分组读感、WI-1 名牌上色、WI-3 对话行差分头像——Demo 无连续同角色对话，经 CDP 现场编写命中"隐藏窗口 rAF 失焦/合成 Enter"脆弱面；为不弄脏用户项目遂止（渲染路径已跑通无崩溃、规则已单测）。

## 风险与已知问题
- WI-5 成员缩进为固定近似，非与名牌像素对齐；成员对话说话人不可行内改（有意）。
- WI-3 对话外观累积为线性 DFS，不建模分支态（卡定位为读感辅助）。
- 过滤态下若原选中行是被隐藏的 staging 行，选择停留在隐藏行直到下次点击（无害）。

## 需要 orchestrator 重点验收的点
1. WI-4 模态 vs "立绘预览上叠内联矩形"的偏离；且最小 UI 不暴露 form 级覆盖的新建。
2. WI-5 视觉未截图——分组/表情 chip/成员缩进的读感需在多说话人场景实测（缩进值 pl-6、导轨、chip 弱化想要人眼确认）。
3. WI-2B 用 charOp 本地化替代 story.command.<id>.label 的机制取舍。
