---
title: "report: U1 阅读层"
type: report
date: 2026-07-26
task: 2026-07-26-008-task-u1-reading-layer.md
branch: feat/ui-u1-reading-layer
commits: 1f9e6c96 (WI-1) · f0a21943 (WI-2) · 26dd22bc (WI-3 + WI-1b)
---
## 1. 对比度与尺寸（demo3 `First Day`，壁纸开启，背板实测 `rgb(11,13,18)`）
| 元素 | 改前 | 改后 @100 | 改后 @45 | 目标 |
|---|---|---|---|---|
| 组内归属导轨（静息、鼠标移开） | 1.27 : 1 | **4.10 : 1** | **4.02 : 1** | ≥3 |
| 类别色条（character／强调色） | 2.87 : 1 | **5.37 : 1** | **5.24 : 1** | ≥3 |
| 类别色条（scene `#8fa9c7`） | — | **6.08 : 1** | **5.99 : 1** | ≥3 |
| 头像 compact／standard／comfortable | 24／24／24 | **28／32／40** | 同 | ≥28、≥40 |

按卡片口径合成后再比；导轨是不透明 `fg-subtle`，截图逐像素采样。
## 2. WI-1b 左边缘：每档只剩一条正文基线

**compact 419.2 · standard 423.2 · comfortable 431.2**，落在这条线上的是：旁白正文、对白正文、
说话人名字、`/face` 标注、每个动作行的内容——跨说话人相同（YouKi／Nattou／Narra／裸名 `Youk` 同数）。
三档之差就是沟槽宽度（头像 28/32/40）。**除基线外只有一条边缘**：行左缘 x=0 的类别色条，即沟槽边界；
沟槽里只有行号、把手、头像/类别牌、导轨、色条，不排文本。嵌套按 depth×20 整体平移"基线+沟槽"，
不新增边缘类。原来的五条（428／432／436／454／488~499）合并为一条：名牌改为该段头行（小一号，
负 margin 抵掉 padding，字形正落基线）；`/face` 的差分头像移进沟槽做导轨上的珠子，标签回到基线。
## 3. 行机械逐条自验（drive.js，520 行夹具，**三档各一遍，17/17 全绿**）

行号连续 ✓ · 单击选行不进编辑 ✓ · `Enter` 进编辑 ✓ · 双击动作行开检查器 ✓ · 单击文本行进编辑并带入
选区 ✓ · `Enter` 继承说话人（新行同组、带导轨）✓ · `Escape` 阶梯（退编辑保留选中；idle 档只关检查器，
与交互模型一致）✓ · `Mod+Z` 撤销 ✓ · 跨行拖选 5 行 ✓ · 把手拖拽重排（位次 1→3）并可撤销 ✓ ·
右键菜单 ✓ · RowActions 插入/删除 ✓ · 从此行播放 ✓ · 把手 hover 才现（20×28，静息 opacity 0）✓ ·
播放头高亮走未改动的 `selectRow`+`scrollRowIntoView`，导轨在选中/激活态转 `bg-primary` 跟随 ✓ ·
**语音指示未能实证**：夹具无 take，组件返回 null；它所在的 `ml-auto` 尾簇未改，同簇的诊断标记／
位置控件／播放按钮三档均正常命中。
## 4. 虚拟化、切档无损、自验

520 行、三档各 8 个滚动位置：**gaps 0、视口无空白**；方向键连走 82 行、查找跳到第 512 行都落在视口内。
**修掉一个真缺陷**：测量缓存按 row id 键控，切档改高度不改 id，缓存活下来 → 行按上一档偏移落位、
互相压字（comfortable 块按 40px 间距叠）；切档时丢弃缓存。切档前后 `storydoc.json` SHA-256 逐字节
相同（`F435AA73…`），选择与滚动位置保留。`yarn lint` 绿；story 范围 vitest 403/403；全仓 9 个失败全在
`src/main`／`src/runtime`／`shared/utils/path`，是已知 win32 基线，本卡零新增；`yarn build:apps:dev` 绿。
## 5. 必须裁决：compact 一屏行数

名字另起一行是**有代价的**：compact 下对白**组头**行 36px → **44px**（后续行、旁白、动作行仍 36px）。
按 748px 视口：非组头行仍 **20 行/屏**（守住基线），但整屏都是"每句换说话人"时只剩 **17 行/屏**。
§6.4「不得低于 20」与 WI-1b 在 compact 下算术上不相容。名字已压到 11px/16px 行高、负 margin 抵掉
padding，44px 是这条路上的下限。请裁决。
## 6. 其他

**没有动 `styles.css`**：该文件有其他 session 未提交的 `.nl-dock-divider` 改动。两个变量
（`--nl-story-avatar`、`--nl-story-handle`）改由 `storyEditorRootStyle` 发布，消费处一律带 fallback。
逐文件 `git add`，未碰他们的 6 个文件。临时 520 行工程（demo3 副本）已从最近项目移除并删目录；
Dev Mode 已停；`editor.surfaceOpacity` 还原 100，demo3 密度还原 compact。
