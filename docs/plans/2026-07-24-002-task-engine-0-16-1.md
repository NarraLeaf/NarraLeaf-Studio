---
title: "task: 引擎 0.16.1 跟进 — Push 信箱盒修复 + 导出 Darkness（narraleaf-react 仓）"
type: handoff
status: draft
date: 2026-07-24
parent: 2026-07-24-001-task-nlr-0-16-migration.md
---

# task: 引擎 0.16.1 跟进（小卡）

你是执行者，工作仓=引擎仓 narraleaf-react（dev_nomen）。背景与两处缺陷的完整分析在 `docs/plans/reports/2026-07-24-nlr-0-16-report.md` §停机裁决——orchestrator 已裁决：两者都在引擎侧根治。

**工作流现实（重要变化）**：npm `narraleaf-react@0.16.0` 已是权威版本，Studio 直接消费 npm；**手拷 dist 到 Studio node_modules 的时代已结束**（`yarn install` 会覆盖）。因此本卡产出只有落到 **npm 0.16.1 发布**后 Studio 才能消费——发布动作归用户，你做到"可发布"为止。Studio 侧的采纳（删 `Slide`、`slide`→native `Push`、`darkness` 接通）**不在本卡**，等 0.16.1 上 npm 后由 orchestrator 出微卡。

- 工作区：主检出若有并发会话脏文件 → 按惯例 `git worktree add`（**删 worktree 前先 `rmdir` 断开 node_modules junction 再 `git worktree remove`——直接删会穿透 junction 清空主仓依赖，已踩过**）。分支 `fix/push-letterbox-and-darkness` off dev_nomen。
- 报告：Studio 仓 `docs/plans/reports/2026-07-24-engine-0-16-1-report.md`（≤40 行）。

## WI-1 `Push` 信箱盒溢出修复

`Push` 目前用 `vw/vh` 平移；转场层 wrapper 是 `inset:0` 的信箱化舞台盒（`Image.tsx` stackStyle），窗口比 ≠ 设计比时溢出露背景。修：平移单位改 `%`（相对转场层自身），参照 Studio 侧 `customImageTransitions.ts` 里保留的 `Slide` 实现（它就是正确答案的样板）。补测试：断言产出的 keyframe/样式用 `%` 而非视口单位。四方向 + `enter/exit` 两相都要覆盖。

## WI-2 导出 `Darkness`

`Darkness` 转场引擎内已实现但未进公开 barrel。导出（含类型），JSDoc 补一句语义（亮度压暗过渡）；确认与 0.16.0 的 Reveal/Mask 体系共存无冲突；补一条构造+参数的冒烟测试。**不许**为导出而改其行为。

## 验证与停机

引擎全量测试绿（310 基线 + 新增）；`tsc`/eslint 干净；CHANGELOG 补 0.16.1 条目（未发布状态照仓内惯例）。**不执行 npm publish、不打 tag**——报告里写"可发布"即止。停机：Push 改 `%` 需要动公开 API 签名（预期不需要——纯内部单位换算）；Darkness 导出暴露出它自身的缺陷（修复超出小卡规模则报告）。
