---
title: "task: Dev Mode 调试面板的固定/浮动两种模式"
type: task
status: ready
date: 2026-07-26
plan: 2026-07-26-004-plan-ui-professionalization.md
---

# Dev Mode 面板：固定 / 浮动可切换

**裁决（用户 2026-07-26）**：面板要能在**固定**与**浮动**之间切换。
**固定模式占据空间**（就是 U0-2 已经做出来的挤压布局：舞台按剩余宽度重新 fit）；
浮动模式浮在舞台上方，不改变舞台尺寸。

这是计划 §8 三个开放问题里的第一个，当初 U0-2 按「先固定挤压，不挡」处理，现在收口。

## 0. 出卡前要重测的基线（**执行者不要跳过**）

本仓四张卡的经验：**每次重测基线都会改写卡**。动手前至少确认：

- 现在的面板挂载点与布局在哪（U4 之后 `StoryRuntimeDebugPanel` 与 `DevModeContent` 都动过，
  `DevModeContent` 还被上游 revert 过一轮）；
- 舞台的 fit 逻辑怎么响应容器宽度变化（U0-2 做挤压时改的那套）；
- 面板宽度当前是写死的还是可变的。

把实测写进报告，与本节不符的地方以实测为准。

## 1. 范围

- **两种模式 + 一个切换控件。** 控件放在面板自己的标题栏（`Story Runtime` 那一行），
  要有可访问名——本轮刚把「可见按钮必须有可访问名」清干净，别又开一个口子。
- **固定模式**：现状不变（挤压，舞台重新 fit）。
- **浮动模式**：面板浮在舞台上，舞台恢复整幅。浮动时至少要能**拖动位置**；
  是否可缩放尺寸交给执行者判断，但不许出现"浮动了却挡死舞台且挪不开"。
- **模式要记住**（跟着 Dev Mode 的会话状态走，与快照选择器同级别的持久化即可）。
- 浮动模式下面板仍然要是**不透明阅读面**（`.nl-editor-surface`），压在舞台上更需要它。

## 2. 明确不做

- 不做多面板停靠系统、不做面板分离成独立窗口。
- 不动面板内部四个 tab 的任何内容（U4/U5 刚验收过，回归必须保持绿）。
- 不改舞台自身的 fit 算法，只是让它在两种模式下拿到正确的可用宽度。

## 3. 共享检出

这个检出多 session 并发。开工前 `git status`；`git commit` 一律带 pathspec；
逐文件 `git add`（禁 `-A`）、禁 `git stash`、禁执行 `git worktree remove`；
提交前 `git branch --show-current`。交报告前做隔离树审计
（`git archive` → 拷 `yarn.lock` → junction node_modules → `yarn lint` + `yarn build:apps:dev`），
**报告里给命令与输出**。

分支 `feat/dev-mode-panel-dock-float`，从当时的 develop 开。

## 4. 判据（orchestrator 所有，出卡时补齐并改前标定）

方向已定，具体断言在标定时定稿：

- 固定模式下舞台宽度 + 面板宽度 ≈ 窗口宽度（面板确实占位）；浮动模式下舞台宽度 ≈ 窗口宽度。
- 两种模式下立绘都完整可见、无裁切（U0-2 的判据不许回归）。
- 浮动模式下面板可拖动，且拖动后仍完全在窗口内。
- 切换控件有可访问名；模式在重开 Dev Mode 后保持。
- 回归：`u4-assert.js` 全绿（A-9 现在也该绿）、`u5-assert.js` 全绿。

## 5. 交付

分支不要自己合并；报告写到 `docs/plans/reports/`；**报告里不要写"验收通过"**。
